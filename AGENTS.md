# AGENTS.md

<!--
- “规范类”是所有项目统一复用的个人开发规范。
- “项目类”由 `bootstrapping-project-foundation` Skill 根据仓库事实填写。
- UI 标准必须基于已确认的代表性原型，由 `standardizing-project-ui` Skill 初始化或重塑。
- 详细方案、测试矩阵、页面原型和阶段进度写入 `docs/`。
-->

## 规范类

### 1. 自主工作与安全边界

- 始终使用简体中文沟通和报告。
- 默认自主完成任务：先阅读相关代码、配置、测试、文档和 Git 状态，再实施和验证。
- 对局部、可逆、低风险的技术选择，采用与现有项目一致、影响更小、容易测试和回滚的方案，不反复询问。
- 不删除、回滚或覆盖用户未提交的代码、文件、数据、文档和本地配置。
- 仅在以下情况暂停并请求确认：
  - 改变核心业务规则、财务口径、权限边界或明显用户行为；
  - 执行生产操作、不可逆数据操作或外部系统写入；
  - 修改生产凭据、访问权限、付费资源、仓库 Secrets 或安全配置；
  - 多种解释会产生明显不同且难以回滚的产品结果；
  - 缺少安全实施所必需的真实信息。

### 2. 代码与变更规范

这些规则用于保持可维护性，不要求为了“规范”增加无意义流程。

- 修改前理解现有实现，遵循已有结构、命名、类型、错误处理和测试风格。
- 只修改完成当前目标所需的内容，不顺手重构、重命名、格式化或清理无关代码。
- 可以进行任务所必需的重构，但要说明原因，并证明外部行为没有被意外改变。
- 优先简单直接的实现，不为一次性需求创建复杂抽象，也不为“以后可能需要”预留大量扩展。
- 删除因本次改动而失效的导入、变量、函数、测试和配置；无关历史问题只报告，不擅自扩大范围。
- 对外部 I/O、网络、认证、权限、用户输入、并发和持久化等可合理预期的失败进行必要处理。
- 新增依赖前确认现有能力无法合理解决，并说明用途、运行位置和影响。
- 不提交凭据、令牌、生产数据、用户文件、缓存、日志、本地数据库和临时导出。

### 3. 按任务风险选择流程

| 类型 | 典型范围 | 执行方式 |
|---|---|---|
| 轻量 | 文案、样式微调、明确小 Bug、单文件修改 | 修改 → 自检 diff → 相关验证 |
| 标准 | 跨文件功能、一般重构、常规 UI、接口调整 | 简短计划 → 实现 → 分阶段验证 → 独立 Reviewer |
| 复杂 | 跨子系统长任务、核心重构、数据库、权限、外部集成、关键 UI 重做 | 方案与验收矩阵 → 方案 Reviewer → 纵向切片实施 → 完成 Reviewer |

- 轻量任务不强制计划文档、独立 Reviewer、Worktree 或 Skill；CI 就绪后仍统一通过 PR 进入 `main`。
- 标准任务只有需要跨会话持续执行时才把计划写入 `docs/`。
- 复杂任务必须把方案和验收矩阵写入 `docs/`，不得只存在于会话历史。

### 4. 长任务与完成定义

#### 4.1 计划必须描述真实缺口

标准或复杂任务的计划应明确：

- 目标、非目标和当前真实缺口；
- 受影响的页面、接口、服务、数据、权限和部署；
- 数据从哪里来、写到哪里、由谁校验；
- 实施顺序、风险和回滚方式；
- 用户可观察的验收条件及验证方法。

计划是实施依据，不是完成证明。

#### 4.2 优先纵向切片

不要先批量做完所有 UI 再补 API，也不要先写大量后端等待页面接入。优先贯通可独立验收的切片：

```text
用户入口 → UI → API / Server Action → 领域逻辑
→ 数据库 / 外部数据源 → 响应状态 → 用户可见结果
```

范围过大时允许分阶段交付，但必须明确尚未完成的阶段。

#### 4.3 验收矩阵

复杂任务的重要验收项至少记录：

| 字段 | 内容 |
|---|---|
| 验收项 | 用户可观察结果或明确工程结果 |
| 实现位置 | 页面、组件、API、服务、任务或迁移 |
| 验证方式 | 测试、浏览器操作、接口、查询、日志或截图 |
| 状态 | `未开始` / `已实现` / `已集成` / `已验证` / `受阻` |
| 证据 | 命令结果、截图、响应、日志、查询或测试名称 |

- `已实现`：代码存在，但尚未证明完整接入。
- `已集成`：上下游已经连接，但尚未完成最终运行验收。
- `已验证`：在目标环境或可靠测试环境取得实际证据。
- `受阻`：缺少外部条件，不能用假实现替代。

#### 4.4 不得虚假完成

功能只有同时满足以下条件，才能报告“完成”：

1. 需求范围内的 UI、API、领域逻辑、数据和权限完整；
2. 正式路径使用真实 API、数据库或明确已联调的数据源；
3. 关键用户路径在浏览器、接口或目标运行环境中实际执行；
4. 相关 lint、类型检查、测试和构建通过，或明确记录不能执行的原因；
5. 达到 Reviewer 触发条件的任务已经复审并关闭阻塞问题。

存在以下情况时不得声称完整完成：

- 正式页面仍使用硬编码业务假数据、运行时 mock、演示数组或测试 fixture；
- 按钮、筛选、分页、保存、导出、状态更新或错误反馈没有真实逻辑；
- 接口不存在，返回固定值、占位响应、空数组、`501`，或应持久化却没有持久化；
- API 已实现但正式页面未使用，或关键路径仍绕过它；
- 数据库迁移、权限、租户隔离、后台任务、错误状态或审计链路缺失；
- 只完成静态原型、Storybook、截图页面或测试 fixture；
- 影响目标的 `TODO`、`FIXME`、stub、placeholder 或 Reviewer 阻塞项仍存在；
- 外部系统没有真实联调，却标记为“已接入”或“可用”；
- 只根据代码 diff 或单元测试判断完成，没有运行关键用户路径。

测试 fixture 必须位于测试或开发专用路径，正式运行代码不得导入。

复杂任务最终提供简洁证据：关键流程截图、接口或数据库验证、实际运行命令、Reviewer 结论、未验证项和风险。

### 5. UI 设计与复刻体系

目标是稳定复刻已确认设计，同时保证真实业务链路完整。

#### 5.1 项目级设计基线

每个项目在“项目类”中指定：

- 全局 UI 基线或设计系统；
- 允许的设计源，例如 Stitch、Product Design 插件、Figma、确认截图或现有正式页面；
- 组件库、字体、图标、默认 viewport；
- 截图/视觉测试工具和验收方式。

新页面、页面重做、Dashboard、复杂表单、关键抽屉和信息架构变更，必须先确认设计源。明确的小视觉 Bug 可直接遵循现有设计系统修改。

#### 5.2 一个页面只有一个冻结设计源

- 每个页面或关键状态只指定一个确认版本，不混用多个原型，也不在编码阶段自行“优化”。
- 冻结记录至少包含：参考截图或导出文件、路由、viewport、字体、图标、关键状态、用户确认时间、允许差异和真实数据来源。
- 设计工具导出的代码只作为视觉和资源参考，不替代项目原有架构、权限、数据流和组件体系。

#### 5.3 固定 UI 执行链

```text
确认设计源和真实数据来源
→ 提取设计契约并冻结
→ 实现页面骨架和关键状态
→ 固定 viewport 浏览器截图
→ 独立视觉 Reviewer
→ 接入真实 API、权限和持久化
→ 真实测试数据复验视觉与功能
→ 关闭视觉与功能阻塞项
```

设计契约至少写明：

- 页面骨架、导航、容器和栅格；
- 字体层级、颜色、间距、圆角、边框和阴影；
- 组件、图标和图片资源；
- 正常、加载、空、错误、权限和关键交互状态；
- 每个数据块的来源及每个操作的后端去向。

#### 5.4 视觉审查顺序

1. 页面骨架、导航、区域顺序、容器宽度和主要布局；
2. 组件尺寸、间距、对齐、表格密度和抽屉结构；
3. 字体、颜色、边框、圆角、阴影、图标和换行；
4. 动效和非关键微细节。

上一级存在明显偏差时，不得用调整颜色或局部像素代替结构修正。

必须实际打开目标路由，在相同 viewport 下保存参考图、实现图和必要差异图。不得只凭代码、DOM 或 Agent 自述“基本一致”通过，也不得为让测试通过而擅自更新设计基准。

#### 5.5 真实数据接入

- 设计阶段标明每个核心数据块的真实来源，不只确认外观。
- 视觉 fixture 只能存在于测试或专用预览入口，正式路由必须使用真实数据适配器。
- 接入真实数据时尽量替换数据层和行为层，不重新改写已通过的整体布局。
- 接入后重新验证加载、空、错误、超长内容、分页、权限和关键交互状态。
- 暂时不能接入真实数据时，页面标记“演示”或“待联调”，任务状态只能是 `受阻` 或 `未完成`。

### 6. Reviewer 规则

独立 Reviewer 有必要，但不作为每个微小修改的固定开销。

#### 6.1 自动触发

以下任务自动派独立 Reviewer：

- 标准任务和复杂任务；
- 新功能、一般以上重构、跨文件 Bug；
- 新页面、页面重做或关键 UI 修改；
- API、数据库、权限、异步任务和外部集成；
- 执行时间较长，或实施过程中多次调整计划；
- 用户明确要求审查。

轻量任务由执行 Agent 自检即可，除非发现风险。

#### 6.2 两个审查阶段

**方案 Reviewer** 只在新模块、核心重构、架构/数据/权限变化、复杂 UI 信息架构和跨子系统长任务时触发，检查范围、依赖、数据链路、遗漏状态、风险和验收方式。

**完成 Reviewer** 根据原始需求、确认方案、实际 diff 和运行证据检查：

- 是否解决完整目标，而不只是执行了计划步骤；
- 是否存在无关修改、遗漏文件和未说明依赖；
- UI 是否匹配冻结设计源；
- 是否仍有假数据、占位接口或未连接操作；
- API、持久化、权限、异步任务和错误路径是否真实贯通；
- 是否意外改变 API、数据库、事件、配置或部署契约；
- 测试和运行证据是否足以支持完成结论。

#### 6.3 审查方式

- Reviewer 使用独立 Agent 或全新审查上下文，默认只读，不直接修改代码。
- Reviewer 必须获得原始需求、确认方案、验收矩阵、实际 diff、验证结果和 UI 参考图。
- 输出分为：`阻塞`、`重要`、`建议`、`通过项`。
- 执行 Agent 修复阻塞和重要问题后，Reviewer 复查相关项。
- 与当前目标无关的优化建议不得扩大为强制范围。

### 7. Skills 与外部工作流

- Skill 是按需工具，不是所有任务必须经过的固定仪式。
- 不在本文件中强制绑定 Superpowers 或其他特定 Skill；模型和工具升级后可选择更合适的等价方法。
- 不得仅因为 Skill 存在就调用，也不得因 Skill 不可用而阻塞任务。
- 明确的小任务直接执行通常优于完整 brainstorming、TDD 或计划流程。
- 对模糊的新产品能力、复杂领域模型和难以回滚的架构决策，可以显式使用 `grill-with-docs` 或等价质询流程，先澄清假设并沉淀术语和 ADR。
- `grill-with-docs` 需要逐问确认，不默认自动触发，也不用于已经明确的常规 Bug、局部重构和自主长任务执行。
- 对已确认方案，优先执行、验证和独立 Reviewer，不重复进行需求访谈。
- 用户明确指定 Skill 时，先确认其目标与项目规则不冲突再使用。

### 8. Git、CI 与 Pull Request

#### 8.1 分支与 Worktree

- 禁止在 `main` 开发或直接推送 `main`；默认在当前目录创建任务分支。
- 分支使用 `feat/`、`fix/`、`refactor/`、`test/`、`docs/`、`chore/`；提交使用对应 Conventional Commit 类型。
- 推送前同步最新 `origin/main`，冲突解决后重新验证。
- CI 状态为 `就绪` 后，所有进入 `main` 的代码和文档变更统一使用 PR，不按任务大小设置例外。
- 默认不用 Worktree。仅在同项目任务并行、隔离高风险实验、跨会话且不能影响当前目录，或用户明确要求时使用；一个任务对应一个 Worktree 和分支，结束后确认无未提交内容再清理。

#### 8.2 CI 与合并治理能力

- PR 不会自动产生 lint、测试和构建；仓库必须先建立真实可运行的 GitHub Actions CI。
- CI 配置放在 `.github/workflows/`；本地与 CI 调用同一组真实脚本。
- CI 状态为：`未建立`、`初始化中`、`就绪`、`降级`、`受阻`。
- 合并治理模式为：
  - `platform-enforced`：平台支持 Required Checks、Branch Protection / Ruleset 和原生 Auto-merge；
  - `agent-gated`：平台强制门禁不可用，由 Agent 在合并前验证 CI 和提交 SHA。
- GitHub Free 私有仓库不能使用受保护分支或原生 Auto-merge 时，自动选择 `agent-gated`；不得因此要求升级 GitHub Pro，不得把仓库改为公开。
- 用户要求“初始化项目”“建立 CI”或“启用 PR 流程”时，视为授权 Agent 创建 CI、验证 PR 和配置当前计划支持的仓库设置；不包含生产部署、Secrets、付费计划升级及破坏性操作。

#### 8.3 首次 CI 建立

新项目或 CI 不可靠时：

1. 扫描运行时、包管理器、锁文件、monorepo、脚本、测试框架、数据库和外部服务。
2. 填写真实验证命令；不得用空脚本、无断言测试、`echo success` 或无条件 `--if-present` 伪造通过。
3. 必要时补齐真实的 `lint`、`typecheck`、`test`、`build` 等项目脚本。
4. 创建 CI：触发 PR 和 `main` 推送；使用锁文件、最小权限、并发取消和超时；不依赖生产数据和生产凭据。
5. 本地运行全部纳入 `CI Gate` 的命令，修复真实问题；无法安全修复的既有问题必须记录。
6. 推送 CI 初始化分支并创建 PR，读取 Actions 日志并迭代，直到真实通过。
7. 检测仓库可见性、GitHub 计划和实际能力，选择 `platform-enforced` 或 `agent-gated`。
8. `platform-enforced`：
   - 配置 PR、Required `CI Gate`、禁止 force push/删除；
   - 启用 squash、原生 Auto-merge 和分支清理。
9. `agent-gated`：
   - 保留 PR、Actions、Reviewer 和 `CI Gate`；
   - 不配置当前计划不支持的 Ruleset；
   - Agent 等待全部检查并核对 `CI Gate`；
   - 合并前确认 head SHA 未变化；
   - 使用 `gh pr merge --match-head-commit <SHA> --squash --delete-branch`。
10. 用安全验证 PR 证明所选模式的“推送 → CI → 合并 → 清理”闭环后标记 `就绪`。

只有 PR 无法创建、Actions 无法运行、没有写权限或真实验证基线无法建立时，才标记 `受阻`。缺少 GitHub Pro 本身不是受阻。

#### 8.4 CI 基线

- CI 汇总检查固定命名为 `CI Gate`；内部 job 可调整。
- 基础 CI 评估依赖安装、lint、类型检查、测试和生产构建；不存在的能力说明原因。
- API、数据库、权限、异步任务、连接器和 UI/E2E 按项目风险加入。
- Required workflow 不使用导致整个工作流不触发的路径过滤；条件优化放在 job 内，并保证 `CI Gate` 总有明确结果。
- `CI Gate` 必须在必需 job 失败、取消或超时时失败；不得用 `continue-on-error` 或忽略退出码掩盖问题。
- PR CI 不部署生产、不连接生产数据库、不读取生产数据。
- 默认使用标准 GitHub-hosted runner，不启用 larger runner、Codespaces 或其他付费产品。
- 使用锁文件确定性安装；缓存只负责提速；设置超时和并发取消。
- 仅在启用 merge queue 时增加 `merge_group` 触发。

#### 8.5 日常无人值守 PR

CI `就绪` 后：

```text
任务分支 → 本地验证 → 按规则 Reviewer → 修复复验
→ commit/push → 创建 PR → 等待 CI
→ 失败则读取日志并修复 → 安全 squash merge
→ 删除远程/本地分支 → 同步本地 main
```

- 不要求用户打开 GitHub、人工批准或点击合并。
- PR 标题使用 Conventional Commit 风格；正文包含目标、关键改动、验证证据、Reviewer 结论和未完成项。
- `platform-enforced` 可使用 GitHub 原生 Auto-merge。
- `agent-gated` 不使用 `--auto`：
  1. 确认 `CI Gate` 已出现；
  2. 等待全部检查完成；
  3. 任一检查失败、取消或超时均不得合并；
  4. 确认 PR head SHA 与通过检查的 SHA 一致；
  5. 使用 `--match-head-commit` 执行 squash merge。
- CI 失败时主动读取日志、修复并重新推送。
- 不使用管理员权限绕过失败门禁，不因改动小而跳过 PR。
- 合并后确认 PR 状态为 `MERGED`，再报告完成。

常用命令：

```bash
git push -u origin HEAD
gh pr create --base main --fill
gh pr checks --watch --fail-fast

# platform-enforced
gh pr merge --squash --auto --delete-branch

# agent-gated
SHA=$(gh pr view --json headRefOid --jq '.headRefOid')
gh pr merge --squash --delete-branch --match-head-commit "$SHA"
```

#### 8.6 高风险远程操作

以下操作仍须明确授权：force push 共享分支、重写历史、删除 tag/release/受保护分支、修改成员权限或 Secrets、使用管理员绕过门禁、生产部署、破坏性数据操作和外部系统写入。

### 9. 最终报告

最终报告简洁区分：

- **已实现**：完成了哪些核心内容；
- **已集成**：哪些真实链路已经连接；
- **已验证**：实际运行了什么，证据是什么；
- **Reviewer**：是否触发，阻塞问题是否关闭；
- **未验证或受阻**：缺少什么条件、影响是什么；
- **Git 状态**：是否提交、推送、合并和删除分支。

不得使用“全部完成”“完全一致”“测试均通过”等无证据结论。

## 项目类

### 1. 项目概况与长期边界

- 项目名称：Hermes Agent 中文站内容仓（`awesome-hermes-agent-zh`）
- 一句话定位：面向中文用户维护 Hermes Agent 实战文档、来源治理、公开图片与可下载 Pack。
- 目标用户和核心结果：中文用户可以按目标完成上手、方案使用、国内落地、迁移判断、排障和参考查询。
- 当前交付阶段：已公开发布并由独立站消费，内容门禁与工程治理基础已建立。
- 明确非目标：
  - 不承载 Next.js 页面实现、生产运行代码或站点部署凭据。
  - 不保存内部派单、运行日志、临时执行看板和私有研究材料。

不可被实现方式改变的业务原则：

- 本仓是 Hermes 中文站正式内容唯一真相源。
- Markdown、route map、Pack manifest 与公开资产必须指向同一套真实内容。
- 新增或移动正式页面必须同步维护 route map；变更 slug 必须协调代码仓重定向。

禁止事项：

- 不提交 Token、私钥、生产数据、本地绝对路径或内部协作日志。
- 未经人工审查不得因上游或第三方版本变化自动改写公开教程。
- 不把计划中页面、未验证能力或第三方宣传写成已发布事实。

### 2. 技术栈与架构职责

- 包管理器与锁文件：pip；CI 依赖固定在 `requirements-ci.txt`
- 运行时：Python 3 与 GitHub Actions
- Web / 客户端：不适用
- API / 后端：不适用
- 数据库：不适用
- 异步任务：GitHub Actions 内容检查、链接检查、上游巡查、第三方巡查和站点同步派发
- 外部系统 / provider：GitHub、NousResearch/hermes-agent 官方来源、已登记模型厂商和第三方方案来源
- 部署平台：本仓不直接部署；通过 repository dispatch 通知 `hermes-zh`

主要目录：

```text
docs/                   正式中文文档与公开图片
packs/                  Pack manifest、安装说明与下载包
governance/             route map、来源、版本和发布合同
scripts/                上游与第三方只读巡查工具
tests/                  巡查与 dispatch 合同测试
.github/workflows/      内容门禁、巡查和站点同步
```

关键调用与放置规则：

- 正式页面写入 `docs`，并在 `governance/site-route-map.yaml` 注册。
- Pack 必须同时具备 manifest、安装入口、下载文件和对应正式文档。
- 站点渲染、SEO实现和永久重定向只在代码仓维护，本仓不复制。

### 3. 项目特有的数据、安全与业务规则

- 核心实体和主数据来源：route、Markdown页面、Pack、来源 registry 与版本 ledger。
- 数据精度、状态、时间或统计口径：只有 `published` 内容供正式站点消费；安装、模型、部署和 Reference 内容必须保留来源复核状态。
- 用户、租户、组织或资源隔离：不适用。
- 凭据和敏感数据边界：dispatch token 只存 GitHub Secret；公开仓不得出现任何部署或 SEO 私钥。
- 外部系统读写边界：巡查默认只读并生成报告；第三方版本变化不得自动改正文。
- 必须由用户明确授权的业务操作：真实创建外部 Issue、修改仓库 Secrets、改变公开内容业务结论和触发生产操作。

### 4. 项目 UI 标准

- UI 标准状态：`不适用`
- 本仓只约束内容图片为公开、可追溯、适合站点消费的资产；站点 UI 标准由代码仓维护。

执行边界：

- 图片视觉变更遵循候选图演示、用户验收、视觉 PASS 后落仓的流程。

### 5. 验证与 CI Profile

当前状态：

- CI 状态：`初始化中`（平台门禁已建立；自动合并与分支清理闭环等待生产发布授权）
- GitHub Actions 入口：`.github/workflows/content-check.yml`（稳定 `CI Gate`）与独立的 `.github/workflows/link-check.yml`
- CI 汇总检查：`CI Gate`
- 仓库能力：个人 Public 仓；Branch Protection、Required Status Checks 与原生 Auto-merge 可用
- 合并治理模式：`platform-enforced`
- `main` Branch protection：已配置 Required `CI Gate`、PR、线性历史，并禁止 force push 和删除
- auto-merge：仓库能力已启用；仅在获得生产发布授权后对发布 PR 启用
- 合并方式：`squash`

真实验证命令：

| 能力 | 命令 | 是否 Required | 适用范围 |
|---|---|---|---|
| 安装 | `python -m pip install -r requirements-ci.txt` | 是 | CI |
| test | `python -m pytest -q` | 是 | 全部 |
| 内容结构 | `content-check` 中的 route/Pack 校验 | 是 | 内容变更 |
| 链接检查 | Lychee workflow | 否 | 独立外链门禁 |
| build | 不适用，由代码仓负责 | 否 | 不适用 |

测试依赖：

- 数据库、缓存、浏览器或其他隔离服务：不适用
- 不允许访问的生产资源：生产站点、部署 Secrets 和外部写接口
- 跨平台验证：Windows 本地与 GitHub Linux Runner 均已验证

### 6. 本地环境、部署与发布

本地开发与验证：

- 本仓没有本地 Web 服务和端口；Markdown 可直接在 GitHub 或编辑器中预览。
- 安装 CI 依赖使用 `python -m pip install -r requirements-ci.txt`。
- 内容变更至少执行 `python -m pytest -q`、`python scripts/normalize_route_order.py` 和 `python scripts/content_quality_check.py`。
- 内容、route map、Pack manifest、来源治理和公开资产必须在同一 PR 中保持一致；不得把站点 generated 文件复制回内容仓。
- `SITE_REPO_DISPATCH_TOKEN` 只允许存在于 GitHub Secret，不得写入仓库、日志、文档或示例。

发布责任边界：

- 本仓是内容唯一真相源，但不直接构建 Next.js、不直接部署 Vercel、不推送 GHCR，也不登录腾讯云服务器。
- 正式站点 canonical 为 `https://hermes-zh.com`；Vercel 承载海外生产线，腾讯云 Docker/Caddy 承载国内生产线，DNSPod 按线路智能解析。
- 本仓只能通过固定的 `repository_dispatch` 合同通知站点仓，不得直接持有 Vercel、GHCR、腾讯云或 DNSPod 凭据。

内容发布流程：

1. 内容 PR 通过 Required `CI Gate`、独立 `link-check` 和 Reviewer 验收。
2. 获得生产发布授权后，以 squash 方式合并到内容仓 `main`；由于后续会触发站点生产链，合并本身属于生产发布动作。
3. `trigger-hermes-zh-content-sync` 对内容相关变更再次执行 route、order、质量和 Pack 校验。
4. 校验通过后向 `zcweah1981/hermes-zh` 发送 `repository_dispatch`，payload 必须包含本仓完整 commit SHA。
5. 站点仓按该 SHA 生成完整内容快照，验证成功后原子提交 `content-lock.json` 与 generated；失败时保持上一版 lock 和生产内容。
6. 站点仓 `main` 更新触发 Vercel 海外构建和 GHCR 镜像；腾讯云在获得授权后受控拉取同一站点 SHA 镜像，DNSPod 继续按既有线路分流。

发布完成标准：

- 内容仓 CI 成功不等于生产发布完成。
- 必须确认站点 lock SHA 与本次内容 commit SHA 一致。
- 必须确认 Vercel 与腾讯云 `/api/healthz` 返回相同的站点 revision 和内容 SHA。
- 必须通过 DNSPod 国内、海外正式线路检查核心页面、Packs、sitemap、robots、llms.txt 和 AI Index。
- 没有双端 revision/content SHA 和正式线路证据时，不得声明内容已上线。

回滚与授权：

- 内容回滚优先 revert 内容提交，或由站点仓受控同步到已验证的历史内容 SHA。
- 回滚同样会触发站点构建与双生产线更新，必须重新完成发布验收。
- 合并内容 PR、触发生产同步、修改 dispatch Secret、修改 DNSPod、更新腾讯云或执行搜索平台真实提交，均必须先获得用户明确授权。
