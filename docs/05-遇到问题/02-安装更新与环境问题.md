# 02-安装 / 更新 / 环境问题

> 🎯 一句话结论：这一页不是带你从头安装 Hermes，而是集中回答“为什么装不上、为什么更新后变怪、为什么命令突然找不到”这类最常见的安装 / 更新 / 环境问题。

如果你现在最焦虑，只先记住一句：

> 只要还没分清是环境层、PATH 层，还是 Hermes 本体层问题，就不要急着删目录重装。

## 这页主要回答什么

这一页集中回答这几类高频问题：

- 为什么 `hermes` 命令找不到？
- 为什么安装脚本一开始就失败？
- 为什么 Windows 上装起来总是不对？
- 为什么更新之后原本可用，现在突然异常？
- 什么情况下该继续排环境，什么情况下该转去查模型 / Provider？

## 先做一个最小判断

如果你现在完全不知道问题卡在哪，先跑这 3 条：

```bash
which hermes
hermes version
hermes doctor
```

这 3 条的作用不是立刻修好，而是先帮你判断：

- 还是环境层问题
- 还是 PATH / shell 没生效
- 还是已经进入 Hermes 本体、Provider、模型层的问题

## ⚡ 快速定位：先看你的问题

如果你不想从头往下读，先按你眼前最像的现象直接跳：

### 🚫 命令起不来
- [❓ 为什么一执行就提示 `hermes: command not found`？](#faq-command-not-found)
- [❓ 为什么安装脚本一开始就失败？](#faq-install-script-fails)

### 🪟 Windows / WSL2
- [❓ Windows 为什么总装不对？](#faq-windows-wsl)

### 🔄 更新后异常
- [❓ 更新之后突然变怪，是不是新版本坏了？](#faq-update-broken)
- [❓ 什么情况先不要重装？](#faq-dont-reinstall)
- [❓ 什么情况才该看手动安装？](#faq-manual-install)

### 🧭 判断问题边界
- [❓ 怎么判断现在还是安装层问题，还是已经进入模型层问题？](#faq-install-vs-model)
- [❓ 中国环境里最容易伪装成“安装失败”的是什么？](#faq-china-env)
- [❓ 如果官方安装器卡在依赖安装提示怎么办？](#faq-installer-stuck)

> 小建议：
> - 先看“命令起不来”
> - 再看“Windows / WSL2”或“更新后异常”
> - 最后再看“判断问题边界”
## ❓FAQ

<a id="faq-command-not-found"></a>

### 1. 为什么我一执行就提示 `hermes: command not found`？
最常见原因不是“完全没装上”，而是：

- shell 还没重新加载
- 你还在旧终端会话里
- `~/.local/bin` 还没进入当前 PATH

先按官方 FAQ 的顺序查：

```bash
source ~/.bashrc
```

如果你用的是 zsh：

```bash
source ~/.zshrc
```

然后再检查：

```bash
which hermes
ls ~/.local/bin/hermes
```

如果还是不行，可以临时补 PATH：

```bash
export PATH="$HOME/.local/bin:$PATH"
```

再重新执行：

```bash
hermes version
```

先做到这一步，再决定要不要继续重装。

<a id="faq-install-script-fails"></a>

### 2. 为什么安装脚本一开始就失败？
这类问题通常先查 3 件事：

1. 你执行的是不是官方完整命令
2. 当前网络 / 代理是否能正常拉取 GitHub 脚本
3. Git 是否已可用

官方一键安装命令是：

```bash
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
```

Git 基础检查：

```bash
git --version
```

如果 `git --version` 都不正常，先修 Git，不要先怀疑 Hermes 自己坏了。

<a id="faq-windows-wsl"></a>

### 3. Windows 为什么总装不对？
因为 Hermes 官方基线很明确：

- 支持：Linux / macOS / WSL2
- **不支持：Native Windows**

也就是说，如果你在 PowerShell / CMD 里直接走官方安装链路，很多异常都不值得继续深挖。

更稳的动作是：

- 先安装 WSL2
- 打开 WSL2 里的 Linux 终端
- 再在 WSL2 里执行官方安装命令

如果你现在还在 Native Windows 里硬装，这一页先给你的结论不是“继续排细节”，而是：

- 先切回 WSL2

<a id="faq-update-broken"></a>

### 4. 更新之后突然变怪，是不是新版本坏了？
不一定。

更新后异常，最常见的几种情况是：

- 你还在旧终端会话里
- shell 没重新加载
- `which hermes` 指向的路径不是你预期的那份
- 其实不是安装层，而是 provider / model / config 层开始报错

最小排查顺序：

```bash
which hermes
hermes version
hermes doctor
```

如果：

- `which hermes` 不对 → 先修 PATH / shell
- `hermes version` 正常、`hermes doctor` 异常 → 说明已经不只是环境层
- 两个都正常 → 你更该去查模型、CLI / TUI、Gateway 或工具层

所以“更新后异常”不自动等于“必须回滚 / 必须重装”。

<a id="faq-dont-reinstall"></a>

### 5. 什么情况先不要重装？
下面这些情况都不建议一上来就删目录重装：

- 安装完只是没 reload shell
- Windows 用户其实跑错到了 PowerShell / CMD
- PATH 里没有 `~/.local/bin`
- 更新后只是旧终端没关
- 真正报错点其实在 provider / model，而不是安装

更稳的判断标准是：

- 只要 `hermes version` 和 `hermes doctor` 还能给出有效输出，就先不要把它判成“完全没装成”

<a id="faq-manual-install"></a>

### 6. 什么情况才该看手动安装？
只有在下面这些情况，你才更应该转去官方 Installation 页里的手动安装部分：

- 一键安装路径反复失败
- 你明确要手动控制 Python / uv / extras
- 你所在环境比较特殊，需要自己装依赖
- 你已经确认不是 shell / PATH / 终端类型问题

如果你只是想尽快先跑起来，不要太早跳进手动安装。

<a id="faq-install-vs-model"></a>

### 7. 我怎么判断现在还是“安装层问题”，还是已经进入“模型层问题”？
一个很实用的分界线是：

- 如果 `hermes` 命令本身都起不来，优先还是安装 / 环境层
- 如果 `hermes version` 正常、`hermes doctor` 也能跑，只是在选模型、API Key、endpoint 时出错，那已经更像模型 / Provider 问题

也就是说：

- 环境层关心的是“命令有没有真正生效”
- 模型层关心的是“命令生效后，为什么推理链路不通”

<a id="faq-china-env"></a>

### 8. 中国环境里最容易伪装成“安装失败”的是什么？
在中文环境里，最常见的伪装项有：

- GitHub 脚本下载慢或失败
- 代理只管浏览器，不管终端
- 云主机本身缺 Git
- WSL2 和 Windows 路径混用
- 装完以后直接跳去折腾 Gateway / Provider，没有先验证基础命令

所以在中国环境里更稳的原则是：

- 先把“终端环境 + Git + 安装器 + shell 生效”这 4 层单独跑通
- 再进入模型、入口和工具层

<a id="faq-installer-stuck"></a>

### 9. 如果官方安装器卡在依赖安装提示怎么办？
官方 issue 里已经有一个很典型的 Windows 相关案例：

- 安装器卡在依赖安装提示时，实际修法不是乱改命令
- 而是切回 **WSL 终端** 再执行安装

所以如果你是在 Windows 场景里遇到这类交互卡住，先不要继续在错误终端里硬试。

## 🔹 官方依据

- [Installation](https://hermes-agent.nousresearch.com/docs/getting-started/installation)
- [Quickstart](https://hermes-agent.nousresearch.com/docs/getting-started/quickstart)
- [FAQ & Troubleshooting](https://hermes-agent.nousresearch.com/docs/reference/faq)
- [Windows WSL2 安装说明](https://learn.microsoft.com/en-us/windows/wsl/install)
- [官方 Issue：安装器卡在依赖安装提示时，Windows 用户应切回 WSL 终端](https://github.com/NousResearch/hermes-agent/issues/6147)

## ✅ 看完这页你应该能立刻回答什么

看完这一页，你应该能直接回答这 4 个问题：

1. 我是不是还在错误环境里装 Hermes？
2. 我的问题是 PATH / shell 没生效，还是安装器根本没跑起来？
3. 我现在该继续排环境，还是该转去查模型 / Provider？
4. 我现在有没有真的必要手动安装或重装？

## ➡️ 下一步

完成后进入：

- 如果你已经确认不是安装层，而是模型 / Provider 层问题，前进到 [03-国内落地 / 01-总览](../03-国内落地/01-总览.md)

如果你想先回到上一阶段入口重新确认位置：

- [01-总览](./01-总览.md)
